# YKI-video-generator 领域与约束

目标是本地单用户创作：输入一句故事，产出可编辑镜头和带中文旁白/字幕的竖屏 MP4。macOS 用于开发和 DEMO；真实推理目标是 Windows 11 / NVIDIA GPU；原始 5090 配置之外新增 4070 Super 12GB / 32GB RAM 实验配置，尚未实测通过。

| 术语 | 含义 |
| --- | --- |
| 项目 | 一个故事与其全部镜头、生成状态、素材和输出，存于 `projects/<id>/` |
| 镜头 | 一段画面提示词、中文旁白、种子及生成版本；当前创建时选 3 或 6 个 |
| 镜头版本 | 对应提示词、旁白、种子、WAV 和 MP4 的完整组合；旧版本保留 |
| 成片 | 按当前选中镜头版本重新合成的 MP4/SRT，文件序号递增 |
| 锁定 | 防止编辑/重做镜头；允许选择历史，不代表锁定人物外貌 |
| DEMO | 色块画面与静音，用于验证应用和媒体流程，不调用模型 |
| 真实模式 | Qwen 规划、CPU Kokoro 旁白、ComfyUI H3 视频、FFmpeg 合成 |
| 环境就绪 | 依赖和资产基础检查通过，不等于真实推理或质量通过 |

单任务队列串行运行。Qwen 规划完成退出后再让 H3 使用 GPU；旁白决定时长，超长旁白要求用户修改。默认 H3 输入为 768×1344，最终输出为 720×1280 / 24 fps / H.264/AAC，真实音轨使用 TTS。

状态通常为 `draft → queued → running → completed`，也可能进入 `failed` 或 `cancelled`；重启将遗留执行状态转为 `interrupted`。恢复复用已完成且素材存在的镜头，不保存扩散采样中间状态。编辑提示词或旁白会让整个镜头待生成，不删除历史素材。

数据保存在本机 JSON 和文件目录，无数据库、云同步或多人合并。个人配置覆盖默认配置；模型和运行时不属于源码交付。H3 权重位于 ComfyUI 的 runtime 子目录，不能假定所有权重都在根 `models/`。

安全约束：绑定 localhost；只停止本程序启动的进程；写请求做来源校验；下载完整性和解压路径校验不得绕过；安装器与应用共用运行环境锁。

当前可确认的是 Mac DEMO 与自动测试。Windows 实机安装、CUDA、真实规划/声音/视频、性能和断网复用都需按验收清单确认。没有参考图、身份锁定、完整 2K 增强、音乐库或自动投稿。

硬件条件于 2026-09-25 按上游资料修订，见 [硬件核查](docs/HARDWARE.md)。不把 32GB 显存或 128GB RAM 当统一最低条件；动态卸载的内存峰值和软件兼容仍需实测。
